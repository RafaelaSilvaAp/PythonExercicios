import math
ang = float(input('Digite ângulo: '))
print('Cosseno = {:.2f}\nSeno = {:.2f}\n Tangente = {:.2f}'.format(math.cos(math.radians(ang)), math.sin(math.radians(ang)), math.tan(math.radians(ang))))
